# PRD: url-launcher 网址启动器 / 书签收纳面板

> 项目代号：url-launcher ｜ 产品经理：许清楚 ｜ 平台：仅 Windows ｜ 形态：系统托盘常驻 + 全局快捷键唤出的纯网址面板
> 技术草案：Tauri 2 + Vite + TypeScript + 本地存储（SQLite 优先 / JSON 兜底），零后端、本地优先

---

## 1. 问题陈述（Problem Statement）

**谁**：Windows 重度用户（开发者、运维、运营、研究者、效率爱好者）。他们在浏览器里积累了 100~1000+ 个常用网址，分散在多个浏览器、书签栏与收藏夹中。

**场景**：工作中频繁需要打开某类固定网址（内部后台、常用 SaaS、文档库、监控面板、常用工具站）。但当前方式低效：
- **浏览器原生书签**：平铺文件夹、无实时过滤、书签栏塞满、跨浏览器不互通、大库难管；
- **万能启动器（uTools / Quicker / Listary）**：书签只是众多插件之一，要先装大软件、学复杂配置、忍受常驻内存与后台崩溃；
- **云书签管理器（Raindrop / Pinboard）**：要登录账号、订阅付费、依赖联网，且无法直接"一键用指定浏览器打开"——打开前还要先进浏览器/扩展。

**为什么不行**：现有方案要么太重（启动器 + 插件生态），要么太云（账号 + 订阅 + 联网），要么太简（原生书签无搜索）。用户真正需要的不是又一个瑞士军刀，而是一个"零配置、本地优先、按下快捷键就能秒开分类网址"的纯面板。

---

## 2. 目标用户（Target Users）

**主要用户画像**
- 身份：Windows 桌面办公用户，开发者 / 运维 / 运营 / 研究 / 效率爱好者
- 年龄：22~40 岁
- 技术水平：中 ~ 高（会用快捷键、会导书签 HTML、能接受托盘常驻软件）
- 痛点：常用网址多（>100）、跨多个浏览器、讨厌账号与订阅、想要"按下就出、点一下就开"
- 频次：每天 20~100 次打开固定网址

**次要用户画像**
- 轻度用户：网址 20~100，想整理收藏夹但不想学复杂工具
- 团队组长：想给团队共享一套内部系统网址集合（MVP 不做多设备同步，但支持导出 JSON 分享）

---

## 3. 竞品分析（Competitive Analysis）

### 直接竞品
1. **uTools** — 插件化效率平台，含"书签管家"插件，Alt+Space 唤出，跨平台。
2. **Quicker** — 鼠标驱动的自动化工具，可建网址捷径，仅 Windows。
3. **Listary** — 文件搜索 + 应用启动器，双击 Ctrl 唤出，仅 Windows。
4. **Cerebro** — 开源跨平台启动器（Electron/React），Ctrl+Space，插件系统。
5. **Wox** — 开源 Windows 启动器，Alt+Space，Everything 驱动。

### 替代方案
- **A. 浏览器原生书签（Chrome / Edge / Firefox）** — 免费、深度集成，但平铺、无实时过滤、跨浏览器割裂、大库混乱。
- **B. 云书签管理器（Raindrop.io / Pinboard）** — 跨设备同步、富组织，但需账号 + 订阅、依赖联网、非本地优先、打开需进浏览器。

### 竞品对比表

| 竞品 | 类型 | 核心功能 | 优势 | 劣势（来自差评 / 评测） | 定价 | 与本项目关系 |
|------|------|----------|------|------------------------|------|------|
| uTools | 万能启动器 | 搜索框 + 插件生态（含书签管家） | 插件丰富、跨平台、Alt+Space | 内存/CPU 高（常驻 30% CPU）、后台易崩溃自动退出、配置黑洞、书签非一等公民（需装插件）、UI 老旧 | 免费 + 会员 ¥30/月 | 直接竞品 |
| Quicker | 鼠标自动化 | 动作面板、网址捷径 | 高度自定义、跨软件联动 | 学习曲线陡、安装后鼠标拖动卡顿、批量导入捷径反人类、不能对网站设专属动作 | 免费 + 专业版 ¥89/年 | 直接竞品 |
| Listary | 文件/应用启动 | 双击 Ctrl 搜索、模糊匹配 | 与资源管理器整合好、快 | 免费版功能有限、双击 Ctrl 易冲突、无插件系统、仅 Windows | 免费 + Pro $20 | 直接竞品 |
| Cerebro | 开源启动器 | Ctrl+Space 统一搜索、插件 | 跨平台、开源、Web 技术友好 | 插件生态弱、Linux 稳定性差、开发慢、偶有性能问题 | 免费开源 | 直接竞品 |
| Wox | 开源启动器 | Alt+Space、Everything 驱动 | 轻量、可定制主题、插件 | 开发停滞、插件安装需 Python/Node、偶有焦点丢失/闪烁、依赖 Everything | 免费开源 | 直接竞品 |
| 浏览器原生书签 | 内置功能 | 书签栏 / 收藏夹 | 免费、零安装、集成 | 平铺文件夹、无实时过滤、跨浏览器割裂、大库混乱 | 免费 | 替代方案 |
| Raindrop.io | 云书签管理 | 集合 / 标签 / 全文搜索 / 缩略图 | 跨平台、视觉好、免费层慷慨 | 纯云依赖、需账号、全文搜索 Pro（$28/年）、无离线、打开需进浏览器/扩展 | 免费 + Pro $28/年 | 替代方案 |
| Pinboard | 云书签管理 | 标签 / 全文搜索 / 归档 | 极快、隐私好、API 强 | 无桌面 App（仅 Web）、UI 陈旧、无免费层、无协作、开发慢 | $22/年 | 替代方案 |

### 差评提炼 → 市场空白
- "常驻久了 CPU 30%、后台卡死 / 自动退出" → 用户要**轻量、稳定常驻**。
- "插件配置黑洞、学习曲线陡" → 用户要**零配置、开箱即用**。
- "书签只是插件之一、要装大平台" → 用户要**书签作为一等公民、单点工具**。
- "云书签要登录 / 订阅 / 联网才能用" → 用户要**本地优先、隐私、离线可用**。
- "打开书签前还要进浏览器 / 扩展" → 用户要**一键用指定浏览器直接打开**。

**→ 市场空白**：一个本地优先、零配置、托盘常驻、全局快捷键秒开、分类 + 实时搜索、点击直开的纯网址面板。这正是 url-launcher 的定位。

---

## 4. 我们的差异化（Differentiation）
- **单点聚焦**：只做"网址收纳 + 秒开"，不做瑞士军刀，无插件生态负担。
- **本地优先**：数据存本地（SQLite / JSON），零后端、零账号、零订阅、隐私可控。
- **极致轻量**：Tauri 2 原生窗口，体积与内存远小于 Electron 类启动器，常驻稳定。
- **秒开体验**：Alt+Space 唤出 → 面板贴右上角 → 实时过滤 → 点击用指定浏览器直开。
- **取用友好**：手动添加 + 书签 HTML 导入 + 浏览器拖拽；新增自动抓标题 / favicon，离线降级默认图标。
- **统一视觉**：Lucide 线性 SVG 图标，16/20/24px 全项目统一，禁用 emoji。

---

## 5. 核心功能列表（RICE 评分 + 优先级）

评分标准：Reach(1-10) 每季受影响用户；Impact(0.25/0.5/1/2/3)；Confidence(50%/80%/100%)；Effort(1-10 人月)；Score = (R × I × C) / E。

| # | 功能 | Reach | Impact | Confidence | Effort | Score | 优先级 | MVP |
|---|------|-------|--------|------------|--------|-------|--------|-----|
| F1 | 全局快捷键唤出面板（默认 Alt+Space，Ctrl+Space 可选但需 IME 警告）+ 托盘常驻 | 10 | 3 | 100% | 2 | 15.0 | P0 | 是 |
| F2 | 网址分类管理（增删改 / 分组 / 拖拽排序） | 10 | 2 | 100% | 3 | 6.7 | P0 | 是 |
| F3 | 实时搜索 / 过滤（标题 / URL / 分类，键盘可达） | 10 | 2 | 100% | 2 | 10.0 | P0 | 是 |
| F4 | 点击用指定浏览器（或系统默认）直接打开 URL | 10 | 2 | 100% | 1 | 20.0 | P0 | 是 |
| F5 | 手动添加网址（仅 URL 必填，可填标题 / 分类） | 10 | 1 | 100% | 1 | 10.0 | P0 | 是 |
| F6 | 浏览器书签 HTML 导入（点击选择 .html 或将其拖入窗口；Chrome/Edge/Firefox） | 8 | 1 | 80% | 2 | 3.2 | P1 | 是 |
| F7 | 从浏览器拖拽"网页超链接"进窗口添加（增强项，待 e2e 实测定稿） | 7 | 1 | 80% | 2 | 2.8 | P2 | 否 |
| F8 | 新增时自动抓取标题 + favicon，离线降级默认图标 | 9 | 1 | 80% | 3 | 2.4 | P1 | 是 |
| F9 | 面板贴右上角、可设位置 / 宽度、置顶 | 8 | 0.5 | 80% | 1 | 3.2 | P1 | 否（可简化） |
| F10 | 快捷键自定义 | 6 | 0.5 | 80% | 1 | 2.4 | P1 | 否 |
| F11 | 分类 / 网址导出导入 JSON（本地备份 / 分享） | 7 | 1 | 80% | 1 | 5.6 | P1 | 否（可后置） |
| F12 | 深色 / 浅色主题切换 | 6 | 0.5 | 100% | 1 | 3.0 | P2 | 否 |
| F13 | 使用统计 / 最近打开排序 | 5 | 0.5 | 50% | 2 | 1.25 | P2 | 否 |
| F14 | 全局模糊搜索（拼音首字母） | 6 | 1 | 50% | 3 | 1.0 | P2 | 否 |

**MVP 保留 F1–F8**（核心闭环：唤出 → 分类 → 搜索 → 添加 → 直开 → 导入 → 拖拽 → 图标），其余进 Backlog。

---

## 6. MVP 范围

**包含**：F1–F6 + F8（全局唤出、分类管理、实时搜索、指定浏览器直开、手动添加、HTML 导入〔含 .html 文件拖入窗口〕、自动抓标题 / favicon + 离线降级）。F7 纯链接拖拽降为增强项（见 §13）。

**明确不做（MVP 及长期）**：
- 云同步 / 多设备（无账号体系）
- 登录账号 / 会员订阅
- 内嵌浏览器（一律外部打开）
- macOS / Linux / 移动端（仅 Windows）
- 插件生态 / 自动化工作流（不做瑞士军刀）
- 全文存档 / 网页快照（区别于 Raindrop）
- 团队协作 / 共享编辑（仅本地导出分享）
- 拼音首字母模糊搜索（Backlog P2）
- 深色主题、使用统计（Backlog）

---

## 7. 验收标准（Given / When / Then）
- **G** 应用已安装并启动；**W** 用户按 Alt+Space（默认，已避开 IME 冲突）；**T** 面板在右上角出现，焦点在搜索框，托盘有常驻图标。
- **G** 面板已打开；**W** 用户输入关键词；**T** 列表实时过滤，仅显示匹配标题 / URL / 分类的项，首条高亮，回车打开。
- **G** 某网址设置了"用 Chrome 打开"；**W** 用户点击该项；**T** 系统调用 Chrome 打开该 URL，面板自动隐藏。
- **G** 用户点"添加"仅填 URL；**W** 确认；**T** 自动尝试抓取标题与 favicon，离线 / 失败时用默认图标与 URL 作为标题，存入默认分类。
- **G** 用户选择浏览器导出的 Bookmarks.html；**W** 导入；**T** 解析出书签与文件夹，按文件夹映射为分类，批量入库，去重。
- **G** 用户将浏览器导出的 Bookmarks.html 文件拖入面板窗口（或点击"导入"选择该文件）；**W** 释放 / 确认；**T** 解析书签与文件夹，按文件夹映射为分类，批量入库，去重。
- **G**（增强项，待 e2e 定稿）用户在浏览器中拖拽一个网页超链接到面板窗口；**W** 释放；**T** 经 HTML5 `text/uri-list` 兜底取到 URL 并新建一条网址（详见 §13 评审修订）。
- **G** 应用首次启动；**W** 无数据；**T** 显示空状态引导（"添加第一个网址"按钮 + 导入入口），不崩溃。
- **G** 用户连续打开面板 8 小时；**W** 观察；**T** 托盘进程内存稳定，无自动退出（对比 uTools 差评）。

---

## 8. 边界条件
- **空状态**：无网址时显示引导，无报错。
- **错误状态**：抓取标题 / favicon 失败 → 离线降级默认图标；导入文件损坏 → 提示并跳过该条。
- **加载状态**：导入大文件（>5000 条）显示进度，不卡死 UI。
- **边界值**：URL 超长截断显示；分类名空 / 重名处理；单分类 1000+ 条滚动性能。
- **并发**：同一 URL 多次添加 → 去重或提示已存在。
- **离线**：无网络时添加仅存 URL，图标用默认，联网后可手动刷新抓取。
- **权限拒绝**：无浏览器可选时回退系统默认；无写入权限时提示数据目录不可写。

---

## 9. 非功能需求

| 类别 | 要求 | 优先级 |
|------|------|--------|
| 性能 | 面板唤出 < 200ms；搜索到渲染 < 200ms（2000 条基线，架构承诺）；万级经虚拟滚动支持，列为性能压测目标；首屏无白屏 | P0 |
| 资源占用 | 常驻内存 < 80MB，CPU 空闲 ~0%；明显优于 Electron 类启动器 | P0 |
| 可用性 | 托盘常驻不闪退；崩溃可恢复数据 | P0 |
| 安全 | 仅本地文件读写；URL 打开用安全 scheme 校验防命令注入；不采集隐私 | P0 |
| 兼容性 | Windows 10 / 11；Chrome / Edge / Firefox / 系统默认浏览器唤起 | P0 |
| 可访问性 | 键盘全程可达（唤出 → 搜索 → 方向键 → 回车）；对比度达标 | P1 |
| 图标规范 | 统一 Lucide 线性 SVG 图标库，16/20/24px；禁用 emoji、禁用紫粉渐变 | P0 |
| 数据 | 本地 SQLite（优先）或 JSON；可导出备份 | P1 |
| 国际化 | 简体中文优先；预留 i18n 接口（不强制） | P2 |

---

## 10. 数据埋点方案（MVP 必埋）

| 事件类别 | 必埋事件 | 说明 |
|----------|----------|------|
| 获客 | app_launch, panel_opened | 启动次数、面板唤出次数（验证快捷键习惯） |
| 激活 | first_url_added, first_import_done | 用户完成首次添加 / 首次导入（激活拐点） |
| 留存 | session_start, url_opened | 每日唤出 / 打开次数（粘性） |
| 转化 | （MVP 无付费，留空） | — |
| 异常 | error_occurred, fetch_failed | 抓取失败率、崩溃（验证离线降级与稳定性） |

命名规范：{对象}_{动作}，如 `url_opened`、`import_completed`、`panel_opened`。属性：`device_id`（匿名）、`timestamp`、`version`、`os_build`。不采集 URL 明文与隐私内容。

---

## 11. 图标与视觉基线（给设计师）

- **图标库**：Lucide（线性、描边 1.5~2px、圆角统一），全项目 16 / 20 / 24px 三档。
- **关键图标映射（文字描述，非 emoji）**：搜索 → 搜索图标；添加 → 加号图标；分类 → 文件夹图标；设置 → 齿轮图标；导入 → 下载 / 文件图标；拖拽 → 箭头图标；默认网址图标 → 地球图标；浏览器 → 显示器图标。
- **配色**：中性灰阶 + 单一品牌强调色（建议蓝 / 青系，**禁用紫色 → 粉色渐变**）。
- **面板**：右上角浮层，圆角、轻阴影；列表行高密度，每行含 [图标] [标题] [分类标签] [打开按钮]。

---

## 12. 待办 / 协作提示（给架构师与设计师）
- **架构师**：Tauri 2 全局快捷键与托盘需 Windows 适配；favicon 抓取建议用系统 WebView 或轻量 HTTP 请求，失败回退默认图标；存储优先 SQLite（如 `tauri-plugin-sql` 或 `rusqlite`）。
- **设计师**：按第 11 节图标与配色基线出高保真；重点设计空状态引导与右上角浮层布局。
- 完整 PRD 已落盘 `PRD.md`，后续评审与范围微调以本文件为准。

---

## 13. PRD 评审修订记录（v1.1，回应架构师评审）

依据架构师高见远对 `ARCHITECTURE.md` §2.2 / §2.4 / §6.1 / §8 的评审，做以下三处修订。三处均不改变产品核心价值主张（本地优先、零配置、纯网址秒开面板），仅修正可行性与默认选型。

### 修订 1：默认全局快捷键由 Ctrl+Space 改为 Alt+Space（P0）
- **原因**：Windows 上 `Ctrl+Space` 是输入法（IME）切换保留键，会被系统吞掉，用户"按了没反应"。架构师在 §2.2 与 e2e 步骤 2/9 已按 `Alt+Space` 设计（与主流竞品 uTools 的肌肉记忆一致）。
- **决策**：出厂默认 = `Alt+Space`；保留 `Control+Alt+Space` 作为无冲突备选；`Ctrl+Space` 仍可在设置中手动选，但录制/保存时前端红字提示 IME 冲突并禁止保存（与 e2e 步骤 9 一致）。
- **影响**：F1 文案、验收标准第 1 条、差异化描述同步更新。属于对原始 brief「默认 Ctrl+Space」的有意偏离，请 team-lead 知悉并确认。

### 修订 2：纯链接拖拽由 MVP 降为增强项（F7: P1→P2，移出 MVP）
- **原因**：WebView2 在 Windows 上把从浏览器拖入的"超链接"可能以文件或 `text/uri-list` 形式投递，存在 MIME 歧义（架构 §2.4）。核心路径改为"书签 .html 文件拖入 + 手动添加"，更可靠。
- **决策**：MVP 包含 F1–F6 + F8；F7（拖网页超链接）降为 P2 增强项，由架构师在 e2e 阶段实测 Chrome/Edge 拖链接真实行为后，用双通道（Tauri `paths` + HTML5 `dataTransfer`）定稿交互。F6 增加".html 文件直接拖入窗口触发导入"作为同解析逻辑的替代入口。
- **影响**：MVP 范围、RICE 表 F6/F7、验收标准拖拽条目同步更新。

### 修订 3：性能基线对齐为「2000 条搜索到渲染 <200ms」（P0）
- **原因**：架构师在 §6.1 / §8 步骤 12 承诺 2000 条流畅（虚拟滚动 + 防抖 120ms + 分页）。
- **决策**：PRD 性能基线由"1000 条内 <50ms"更新为"搜索到渲染 <200ms（2000 条基线）"；万级（10k+）经虚拟滚动仍可行，列为性能压测目标而非 P0 验收门槛。
- **影响**：§9 非功能需求「性能」行同步更新。

> 以上修订已与架构方案 `ARCHITECTURE.md` 对齐，PRD 与架构互为契约，可进入实现阶段。
